<main>
  {{#with housing}}
    <section id='deatils-page'>
      <div class='wrapper'>
        <div class='product-img'>
          <img src='{{image}}' />
        </div>
        <div class='product-info'>
          <div class='product-text'>
            <div class='product-text'>
              <h1>Name: {{name}}</h1>
              <h2>Type: {{type}}</h2>
              <h4>Year: {{year}}</h4>
              <h4>City: {{city}}</h4>
              <p>
                Description:{{description}}
              </p>

              <!-- if there are already tenants of the housing, separate their names with a comma and a space ", "  -->
              {{#if rentCount}}
                <p>People rented this housing: {{usernames}}</p>
              {{else}}
                <!-- If not display: -->
                <p>People rented this housing: There are no tenants yet.</p>
              {{/if}}
            </div>
          </div>

          <!-- if there is no registered user, do not display buttons-->
          {{#if loggedUser}}
            <div class='product-btn'>
              <!-- Only for registered user and creator of the housing offer-->
              {{#if isOwner}}
                <a href='/site/{{_id}}/details/edit' class='edit'>Edit</a>
                <a href='/site/{{_id}}/details/delete' class='remove'>Delete</a>
              {{else}}
                <!-- logged in user with available pieces-->
                {{#unless noPieces}}
                  {{#if notRented}}
                    <a href='/site/{{_id}}/details/rent' class='rentHome'>Rent a
                      home, available
                      {{availablePieces}}
                      housing</a>
                  {{else}}
                    <p class='alRentHome'>You have already rent this home</p>
                  {{/if}}
                  <!-- logged in user with no available pieces-->
                {{/unless}}
                {{#if noPieces}}
                  <p class='no-housing'>No Housing Available!</p>
                {{/if}}
              {{/if}}
            </div>
          {{/if}}
        </div>
      </div>
    </section>
  {{/with}}
</main>